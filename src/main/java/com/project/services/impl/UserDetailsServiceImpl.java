package com.project.services.impl;

//@Service
//@Component
public class UserDetailsServiceImpl /* implements UserDetailsService */ {
	/*
	 * 
	 * @Autowired private LoginRepo loginRepo;
	 * 
	 * @Override public UserDetails loadUserByUsername(String username) throws
	 * UsernameNotFoundException { LoginEntity login =
	 * this.loginRepo.findByUsername(username);
	 * 
	 * if(login!=null) { return new
	 * org.springframework.security.core.userdetails.User(login.getUsername(),
	 * login.getPassword(), new ArrayList()); } return null; }
	 * 
	 */}
